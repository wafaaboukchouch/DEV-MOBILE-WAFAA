name: wafaa_bot
description: "Séance 5 — WafaaBot : Chatbot IA multiplateforme (Groq API + Flutter) — Wafaa Boukchouch MII BDCC ENSET"
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_dotenv: ^5.1.0

flutter:
  uses-material-design: true
  assets:
    - .env
